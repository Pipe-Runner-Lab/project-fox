import styled from "styled-components"

export const DashboardContainer = styled.div`

`;

export const Heading = styled.div`
  position: relative;
  display: flex;
  height: 80px;
  align-items: center;
  border-bottom: 1px solid rgba(black , 0.05);
  box-shadow: 0px 12px 55px 12px rgba(207,216,220,1);
  -webkit-box-shadow: 0px 12px 55px 12px rgba(207,216,220,1);
  -moz-box-shadow: 0px 12px 55px 12px rgba(207,216,220,1);
;
`;

export const Header = styled.div`
margin: 40px 0;
`;
